# Generated by build.sh + run.sh / run-dosbox.sh
build/
freedos-bigint.img
qemu-bigint.log
# run-dosbox.sh copies BIGINT.EXE into this dir for DOSBox-X to mount,
# and DOSBox-X writes RIG.LOG via the autoexec redirect.
BIGINT.EXE
RIG.LOG
