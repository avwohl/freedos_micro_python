# Generated by build.sh + run.sh / run-dosbox.sh
build/
test-server.crt
test-server.key
freedos-tls.img
qemu-tls.log
qemu-tls-smoke.pcap
tls-server.log
# run-dosbox.sh stages TLS.EXE here for DOSBox-X to mount and writes
# RIG.LOG via the autoexec redirect.
TLS.EXE
RIG.LOG
# run-crynwr.sh artifacts (FreeDOS image + QEMU log + pcap).
freedos-tls-crynwr.img
qemu-tls-crynwr.log
qemu-tls-crynwr.pcap
